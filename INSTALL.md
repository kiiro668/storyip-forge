# 安装与使用

当前 `v0.17.0` 为 StoryIP Forge 的公开基线版本。

## 获取仓库

```bash
git clone https://github.com/kiiro668/storyip-forge.git
cd storyip-forge
```

仓库通过 `.codex-plugin/plugin.json` 暴露插件元数据，并以 `skills/storyip-forge/SKILL.md` 作为主控入口。

后续专业子 Skill、执行脚本、参考合同和测试会继续以常规 Git 提交补齐。

## 版权边界

仓库不包含第三方小说正文、目标 IP 官方设定库、角色素材、声音参考或品牌资产。使用者需自行确保输入内容与素材拥有合法授权。
